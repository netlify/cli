import { describe, it } from 'vitest'

import { getGlobalConfigStore, resetConfigCache } from '../../../../src/utils/global-config/main.js'

describe('getGlobalConfig', () => {
  it.todo('retrieves the global config')
  it.todo('caches a previously read global config')
})

describe('resetConfigCache', () => {
  it.todo('resets the global config cache')
  it.todo('succeeds when no config is cached')
})
