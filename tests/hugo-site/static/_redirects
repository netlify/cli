# Redirects from the first URL to the second URL, 301 is default
# /redirect-test			/test
# For forwarding http to https-www directly to reduce number of forwards
# http://domain.com/* https://www.domain.com/:splat 301!

/thanks/ /


/api/*  /.netlify/functions/:splat 200

# Must be last in list or all redirects will 404
/* /404.html 404